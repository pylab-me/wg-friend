[package]
name = "wg-friend"
version = "0.4.0"
edition = "2021"
description = "Semantic WireGuard/BoringTun lifecycle and client helper"
license = "MIT"

[dependencies]
anyhow = "1"
clap = { version = "4", features = ["derive"] }
qrcode = "0.14"
